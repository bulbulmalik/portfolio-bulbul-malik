# RAG Knowledge Base Overview

This mini RAG system uses a semantic embedding model to transform documents and user questions into vectors. It archives the most relevant content from the knowledge base and then generates a clean answer from the retrieved context.

In a full production system, this would be connected to company manuals, product documentation, or internal knowledge sources.
